import styles from './Map.module.scss';

const Map = () => {
    return (
        <section className={`box placeholder ${styles.map}`}>
            <h1>For map</h1>
        </section>
    )
}

export default Map
